const CreateHero = require('src/app/hero/CreateHero');

describe('App :: hero :: CreateHero', () => {
  describe('when hero is valid', () => {
    test('should create the Hero and returns a Ok output', () => {

    });
  });
});
