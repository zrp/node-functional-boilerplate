const enumsModel = require('./enums.model');
const heroModel = require('./hero.model');
const villainModel = require('./villain.model');

module.exports = {
  enumsModel,
  heroModel,
  villainModel,
};
